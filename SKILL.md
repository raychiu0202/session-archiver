---
name: session-archiver
description: 智能会话存档工具，自动检测新会话并自动归档上一个会话。支持手动触发："保存会话", "存档", "归档", "archive session", "save conversation"。存档至 ~/Documents/my_ai_archive/conversations/，并维护 MANIFEST.md 索引表。自动生成中文标题（10-30 字）、摘要（200+ 字）和核心内容文档。通过 session-archiver-state.json 防止重复存档。
---

# 会话存档工具

智能会话存档工具，自动检测新会话创建并归档上一个会话。存档文件包含分析整理后的核心内容文档，而非原始聊天记录，便于快速回顾。同时也支持手动触发。

## 需要的外部命令

- `mkdir` - 创建目录
- `echo` - 追加内容到 MANIFEST.md
- `date` - 生成时间戳
- `grep` - 在 MANIFEST.md 中检查重复

## 触发方式

**手动触发：**
- 中文："保存会话", "存档", "归档", "生成会话清单"
- 英文："archive session", "save conversation", "export chat"

**自动触发（基于心跳）：** 每次心跳时，自动检测是否创建了新会话，如果检测到新会话则自动归档上一个会话，无需提示。

## 配置

存档根目录（默认：`~/Documents/my_ai_archive/`）。可通过环境变量覆盖：
```bash
export SESSION_ARCHIVER_ROOT="~/custom/path"
```

## 状态文件

此 skill 在 `~/.openclaw/workspace/session-archiver-state.json` 中维护状态：
```json
{
  "lastTrackedSessionId": "agent:main:xxx",
  "lastCheckTime": 17762442800000
}
```

- 每次心跳时读取此文件以检测新会话
- 每次自动存档操作后更新
- 首次运行时自动创建（如果不存在）

## 工作流程

### 阶段 1：检测新会话（心跳自动触发）

1. 读取状态文件：`~/.openclaw/workspace/session-archiver-state.json`
   - 如果不存在，创建空状态（没有 lastTrackedSessionId）
2. 使用 `sessions_list` 列出最近会话，设置 `activeMinutes=1440`（最近 24 小时）
3. 识别当前运行的会话（列表中的第一个）
4. 与 `lastTrackedSessionId` 比较：
   - **如果不同**：检测到新会话！上一个会话已结束 → 继续存档
   - **如果相同**：没有新会话，不执行任何操作（跳过整个流程）

### 阶段 2：确定目标会话（自动触发）

当检测到新会话时：
1. "上一个会话"是 `lastTrackedSessionId` 中的那个
2. 使用它作为存档目标
3. 如果 `lastTrackedSessionId` 为空/null，跳过（首次运行，暂无内容可存档）

### 阶段 3：检查重复

在存档前，检查 MANIFEST.md 中是否已存在该会话 ID：
```bash
grep -q "| $SESSION_ID |" "$MANIFEST_PATH"
```
如果找到，静默跳过（已在之前的运行中存档）。

### 阶段 4：收集会话数据

1. 获取会话信息：
   - 会话 ID：来自 `lastTrackedSessionId`（上一个会话）
   - 时间戳：使用 `date '+%Y-%m-%d_%H-%M-%S'`
   - 模型：来自会话元数据（如果可用）

2. 使用会话 key 通过 `sessions_history` 获取完整对话

### 阶段 5：生成标题、摘要和核心内容（自动触发）

自动生成，无需询问用户：

1. **标题**：简洁的中文总结（10-30 字），捕获主要主题
2. **摘要**：详细的中文总结（200+ 字），覆盖对话的关键点
3. **核心内容文档**：分析整理后的结构化文档（非原始聊天记录）

详细逻辑见"自动生成规则"部分。

### 阶段 6：写入存档文件

1. 确保目录存在：
   ```bash
   mkdir -p "$ARCHIVE_ROOT/conversations"
   ```

2. 写入 markdown 文件：`$ARCHIVE_ROOT/conversations/YYYY-MM-DD_HH-MM-SS_session.md`

   格式：
   ```markdown
   # {标题}

   - **会话 ID**: {session_id}
   - **存档时间**: {timestamp}
   - **模型**: {model}

   ---

   ## 摘要

   {summary}

   ---

   ## 核心内容

   {structured_content}

   ---
   *由 session-archiver skill 自动存档*
   ```

### 阶段 7：更新 MANIFEST.md

1. 读取现有 MANIFEST.md 检查格式
2. 如果缺失或损坏，重新创建表头：
   ```markdown
   # 会话存档清单

   | 会话ID | 存档时间 | 标题 | 摘要 | 文件路径 |
   |--------|----------|------|------|----------|
   ```

3. 追加新行：
   ```bash
   cat >> "$MANIFEST_PATH" << EOF
   | $SESSION_ID | $TIMESTAMP | $TITLE | $SUMMARY | $REL_PATH |
   EOF
   ```

### 阶段 8：更新状态并确认

1. 使用当前会话 ID 更新状态文件：
   ```json
   {
     "lastTrackedSessionId": "{current_session_id}",
     "lastCheckTime": {current_timestamp}
   }
   ```

2. 对于自动触发，回复确认信息：
   ```
   ✅ 自动存档完成：上一个会话已存档至 `$ARCHIVE_ROOT/conversations/YYYY-MM-DD_HH-MM-SS_session.md`，清单已更新。
   ```

### 手动触发工作流程（当用户显式触发时）

如果用户明确说"保存会话"或类似内容：

1. 使用相同的阶段 1-4，但以不同方式识别"上一个会话"：
   - 列出最近会话
   - 找到最近的一个不是当前正在运行的会话
2. 询问用户标题、摘要和核心内容（手动触发时跳过自动生成）
3. 继续执行阶段 6-8

## 错误处理

- **无上一个会话**：不执行任何操作（自动触发时无需报错）
- **重复的会话 ID**：静默跳过（已存档）
- **文件写入失败**：记录错误并通知用户
- **MANIFEST 损坏**：追加前重新生成表头
- **空对话**：仍使用最小内容存档
- **状态文件缺失**：首次运行时自动创建（不是错误）

## 自动生成规则

自动生成标题、摘要和核心内容时，阅读完整的对话历史：

### 1. 生成标题

1. 识别主要主题、任务或目的
2. 生成简洁的中文标题（10-30 字符）
3. 重点关注：讨论了什么、构建了什么、解决了什么问题
4. 示例："开发会话存档 Skill"、"优化数据库查询性能"、"设计用户认证流程"

### 2. 生成摘要

1. 阅读完整的对话历史（所有用户和助手消息）
2. 提取关键信息：
   - 用户的目标/请求是什么？
   - 讨论了哪些解决方案/方法？
   - 最终结果或决定是什么？
   - 任何重要的技术细节或决定
3. 以叙述形式撰写详细的中文摘要（200+ 字）
4. 结构：背景需求 → 讨论过程 → 最终结论
5. 如果对话太短（少于 200 字可能），自然包含所有内容

### 3. 生成核心内容文档

**核心原则**：这是分析整理后的文档，不是原始聊天记录。目的是便于快速回顾会话的核心内容。

根据对话类型，采用不同的整理方式：

#### 场景 A：技术实现/开发类

**结构：**
```markdown
### 背景需求
- 用户提出的目标/问题
- 技术要求和约束条件

### 解决方案
- 采用的技术方案/架构
- 关键决策和选择理由
- 实施步骤和流程

### 关键技术细节
- 重要配置参数
- 代码实现要点
- 遇到的技术挑战和解决方案

### 最终结果
- 完成的功能/任务
- 验证和测试结果
- 后续优化方向
```

**示例：**
```markdown
### 背景需求
用户需要开发一个 OpenClaw Skill，实现会话自动存档功能。要求支持自动检测会话切分和手动触发两种模式，存档格式为 Markdown，并维护一个 MANIFEST.md 清单表格。

### 解决方案
采用 session-archiver-state.json 追踪会话状态，通过心跳机制自动检测新会话。存档文件包含标题、摘要和整理后的核心内容，便于快速回顾。清单表格记录会话 ID、时间、标题、摘要和文件路径。

### 关键技术细节
- 使用 sessions_list 和 sessions_history API 获取会话数据
- 通过 grep 检查 MANIFEST.md 避免重复存档
- 自动生成中文标题（10-30 字）和摘要（200+ 字）
- 核心内容按场景分类整理，非原始聊天记录

### 最终结果
实现了完整的 session-archiver skill，已发布到 GitHub 并安装到本地环境。测试验证了自动触发、手动触发、去重机制和清单更新功能均正常工作。
```

#### 场景 B：讨论/咨询类

**结构：**
```markdown
### 讨论主题
- 核心议题和问题
- 参与者的观点和立场

### 关键观点
- 主要论点和理由
- 不同方案的对比分析
- 达成共识或有争议的地方

### 结论与建议
- 最终结论或决策
- 后续行动计划
- 值得参考的要点
```

**示例：**
```markdown
### 讨论主题
讨论项目架构设计方案，比较微服务架构和单体架构的优劣，适用于当前业务场景。

### 关键观点
- 微服务架构：独立部署、技术栈灵活，但运维复杂度高
- 单体架构：开发简单、部署容易，但扩展性受限
- 考虑到团队规模和业务复杂度，建议采用渐进式架构

### 结论与建议
初期使用单体架构快速迭代，业务增长后逐步拆分微服务。需要完善监控和日志系统，为后续重构做准备。
```

#### 场景 C：问题排查/故障解决类

**结构：**
```markdown
### 问题描述
- 现象和错误信息
- 影响范围和严重程度

### 排查过程
- 检查步骤和验证方法
- 排除的可能原因
- 发现的关键线索

### 根本原因
- 问题产生的根本原因
- 触发条件和环境因素

### 解决方案
- 修复措施和实施步骤
- 验证结果
- 预防措施
```

**示例：**
```markdown
### 问题描述
系统响应缓慢，部分请求超时。错误日志显示数据库连接池耗尽，影响所有写入操作。

### 排查过程
1. 检查数据库慢查询日志，发现部分查询执行时间过长
2. 分析应用监控数据，确认数据库连接池使用率接近 100%
3. 排查代码逻辑，发现存在连接未正确释放的情况

### 根本原因
代码中存在数据库连接泄漏，连接未及时归还给连接池。在高并发场景下，连接池资源被耗尽。

### 解决方案
修复连接释放逻辑，添加连接池监控告警。优化慢查询，调整连接池大小。验证后系统恢复正常响应。
```

#### 场景 D：规划/设计类

**结构：**
```markdown
### 目标范围
- 项目目标和预期成果
- 约束条件和边界

### 方案设计
- 整体架构和设计思路
- 关键模块和功能
- 技术选型和理由

### 实施计划
- 阶段划分和里程碑
- 资源需求和时间估算
- 风险评估和应对策略

### 交付物
- 文档和代码清单
- 验收标准
```

#### 场景 E：学习/探索类

**结构：**
```markdown
### 学习目标
- 想要了解或掌握的内容
- 背景和动机

### 学习过程
- 探索的路径和方法
- 尝试的工具和技术
- 遇到的困难和解决方式

### 关键知识点
- 核心概念和原理
- 重要发现和洞察
- 最佳实践和注意事项

### 总结与收获
- 达成的学习目标
- 可应用的知识和技能
- 后续深入学习的方向
```

### 通用规则

1. **去重**：相同观点只记录一次，避免重复
2. **提炼**：从多轮对话中提炼核心信息，不记录琐碎细节
3. **结构化**：使用标题和列表组织内容，便于快速浏览
4. **中文表达**：使用清晰的中文表达，保留必要的技术术语
5. **完整性**：确保涵盖会话的主要内容和结论
6. **可读性**：使用适当的格式（列表、代码块、引用）提升可读性

### 处理特殊情况

- **空对话或无实质内容**：标注"本次会话无实质内容"
- **多主题讨论**：按主题分段整理，使用二级标题
- **未完成或中断**：标注"会话未完成，以下为已讨论内容"
- **包含敏感信息**：自动过滤或脱敏敏感数据（密码、密钥等）

## 示例输出

### 标题
"OpenClaw 会话存档 Skill 开发"

### 摘要
"用户请求开发一个 OpenClaw Skill，实现会话自动存档功能。讨论了触发方式（自动检测会话切分+手动触发）、存档格式（Markdown）、清单维护（MANIFEST.md 表格）等技术细节。最终实现了包含 SKILL.md、辅助脚本和中英文 README 的完整项目，已发布到 GitHub 并安装到本地环境。测试验证了存档文件生成、清单更新和去重功能均正常工作。后续优化了核心内容生成逻辑，要求标题为总结性中文标题（10-30 字），摘要为详细中文摘要（200+ 字），核心内容为分析整理后的结构化文档，便于快速回顾会话。"

### 核心内容
```markdown
### 背景需求
用户需要开发一个 OpenClaw Skill，实现会话自动存档功能。要求支持自动检测会话切分和手动触发两种模式，存档格式为 Markdown，并维护一个 MANIFEST.md 清单表格。核心要求是存档文件包含分析整理后的核心内容文档，而非原始聊天记录，便于快速回顾会话内容。

### 解决方案
采用 session-archiver-state.json 追踪会话状态，通过心跳机制自动检测新会话。存档文件包含标题、摘要和整理后的核心内容。清单表格记录会话 ID、时间、标题、摘要和文件路径。核心内容根据对话场景分类整理，采用结构化的格式。

### 关键技术细节
- 使用 sessions_list 和 sessions_history API 获取会话数据
- 通过 grep 检查 MANIFEST.md 避免重复存档
- 自动生成中文标题（10-30 字）和摘要（200+ 字）
- 核心内容按场景分类（技术实现/开发类、讨论/咨询类、问题排查/故障解决类、规划/设计类、学习/探索类）
- 核心内容整理原则：去重、提炼、结构化、中文表达、完整性、可读性

### 最终结果
实现了完整的 session-archiver skill，已发布到 GitHub 并安装到本地环境。测试验证了自动触发、手动触发、去重机制和清单更新功能均正常工作。后续根据用户反馈优化了核心内容生成逻辑，确保存档文件便于快速回顾会话的核心内容。
```
